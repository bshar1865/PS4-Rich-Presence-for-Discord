<Window x:Class="PS4RichPresence.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        Title="PS4 Rich Presence" Height="600" Width="400"
        Icon="/app.ico"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <ContextMenu x:Key="TrayMenu">
            <MenuItem Header="Show Window" Click="ShowWindow_Click"/>
            <Separator/>
            <MenuItem Header="Connect" Click="Connect_Click"/>
            <Separator/>
            <MenuItem Header="Exit" Click="Exit_Click"/>
        </ContextMenu>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Status Panel -->
        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="PS4Status" Text="PS4: Disconnected" Grid.Column="0" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock x:Name="DiscordStatus" Text="Discord: Disconnected" Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center"/>
            </Grid>
        </StackPanel>

        <!-- Game Info -->
        <TextBlock Grid.Row="1" x:Name="GameName" Text="No game running" 
                   HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <!-- Game Image -->
        <Border Grid.Row="2" BorderBrush="LightGray" BorderThickness="1" Margin="0,0,0,10">
            <Image x:Name="GameImage" Stretch="Uniform"/>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="ConnectButton" Content="Connect PS4" Click="ConnectButton_Click" Width="100" Margin="0,0,10,0"/>
            <Button Content="Settings" Click="Settings_Click" Width="100" Margin="0,0,10,0"/>
            <Button Content="Edit Game Info" Click="EditGame_Click" Width="100"/>
        </StackPanel>

        <!-- Tray Icon -->
        <tb:TaskbarIcon x:Name="TrayIcon"
                        IconSource="/app.ico"
                        ToolTipText="PS4 Rich Presence"
                        ContextMenu="{StaticResource TrayMenu}"
                        TrayLeftMouseDown="TrayIcon_Click"/>
    </Grid>
</Window> 